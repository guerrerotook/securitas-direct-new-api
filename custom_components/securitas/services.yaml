# DEPRECATED. These services are aliases for verisure_owa.* services and
# will be removed in v6.0.0. Update your automations to use the new names.

force_arm:
  description: |
    DEPRECATED — use verisure_owa.force_arm instead.
    Force arm the alarm even when sensors are open.
  target:
    entity:
      integration: verisure_owa
      domain: alarm_control_panel

force_arm_cancel:
  description: |
    DEPRECATED — use verisure_owa.force_arm_cancel instead.
    Cancel a pending force-arm.
  target:
    entity:
      integration: verisure_owa
      domain: alarm_control_panel
